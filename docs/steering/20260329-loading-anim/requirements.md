# ローディングアニメーション

## 背景

Pyodideロード中に表示される「Python環境を読み込み中...」メッセージが静的テキストで味気ない。子ども向けに待ち時間を楽しくするアニメーションを追加する。

## 要件

- テキストにシマーグラデーション（`background-clip: text`で左→右に色が流れる）
- 各文字が波打つようにバウンド（ウェーブアニメーション）
- CSS only（JSのアニメーション処理は不要、文字のspan化のみJS）
- `prefers-reduced-motion`対応
- ロード完了後は従来通り`hidden`で非表示
