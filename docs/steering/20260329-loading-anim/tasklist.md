# ローディングアニメーション — タスクリスト

## タスク

- [x] 1. `app/style.css`: `#status`の既存スタイル変更、`.loading`/`.fade-out`クラス追加、keyframes 3つ追加（shimmer, bounce, fadeOut）、prefers-reduced-motion対応
- [x] 2. `src/main.js`: ステータス表示のspan化、`.loading`クラス制御、フェードアウト処理（reduced-motion分岐含む）
- [x] 3. ビルド＋動作確認
