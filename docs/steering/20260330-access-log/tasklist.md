# タスクリスト

## 🚨 タスク完全完了の原則

**このファイルの全タスクが完了するまで作業を継続すること**

### 必須ルール
- **全てのタスクを`[x]`にすること**
- 「時間の都合により別タスクとして実施予定」は禁止
- 「実装が複雑すぎるため後回し」は禁止
- 未完了タスク（`[ ]`）を残したまま作業を終了しない

### 実装可能なタスクのみを計画
- 計画段階で「実装可能なタスク」のみをリストアップ
- 「将来やるかもしれないタスク」は含めない
- 「検討中のタスク」は含めない

### タスクスキップが許可される唯一のケース
以下の技術的理由に該当する場合のみスキップ可能:
- 実装方針の変更により、機能自体が不要になった
- アーキテクチャ変更により、別の実装方法に置き換わった
- 依存関係の変更により、タスクが実行不可能になった

スキップ時は必ず理由を明記:
```markdown
- [x] ~~タスク名~~（実装方針変更により不要: 具体的な技術的理由）
```

### タスクが大きすぎる場合
- タスクを小さなサブタスクに分割
- 分割したサブタスクをこのファイルに追加
- サブタスクを1つずつ完了させる

---

## フェーズ0: ブランチ準備

- [x] mainに未マージのfeatureブランチがないか確認
- [x] 作業用ブランチを作成（`feature/20260330-access-log`）

## フェーズ1: 依存ライブラリとプロジェクト設定

- [x] `npm install geoip-lite` を実行
- [x] `.gitignore` に `logs/` を追加

## フェーズ2: server.js にログ記録を実装（TDD）

- [x] テストファイル作成（test/access-log.test.js 等）
  - [x] テスト: GETリクエスト時にaccess.jsonlにレコードが追記される → Red
  - [x] テスト: レコードに必須フィールド（ts, method, url, status, ua, ref, ip, country）が含まれる → Red
  - [x] テスト: 404レスポンス時にstatus=404が記録される → Red
  - [x] テスト: GeoIP解決不能時にcountryがnullになる → Red

- [x] server.jsにログ記録処理を実装 → Green
  - [x] サーバー起動時にlogsディレクトリを自動作成（`fs.mkdirSync`）
  - [x] geoip-liteのrequireと初期化
  - [x] リクエスト情報の収集（method, url, ua, referer, ip）
  - [x] IPアドレス取得（x-forwarded-for 対応）
  - [x] GeoIP国コード解決
  - [x] レスポンス完了後にステータスコード確定
  - [x] JSONレコード構築とappendFile追記

- [x] logsディレクトリへの直接アクセスを拒否するガード追加

## フェーズ3: 品質チェックと修正

- [x] すべてのテストが通ることを確認
- [x] ビルドが成功することを確認
  - [x] `npm run build`
- [x] 手動動作確認
  - [x] サーバー起動 → ブラウザでアクセス → access.jsonlにレコードが出力される
  - [x] 存在しないURLにアクセス → status=404が記録される
  - [x] logs/access.jsonlに直接アクセス → 拒否される

## フェーズ4: mainマージとドキュメント更新

- [ ] featureブランチをmainにマージする
- [x] 実装後の振り返り（このファイルの下部に記録）

---

## 実装後の振り返り

### 実装完了日
2026-03-30

### 計画と実績の差分

**計画と異なった点**:
- server.jsに `module.exports = server` を追加し、テストからrequireできるようにした（既存の動作に影響なし）
- `process.env.PORT` 対応を追加し、テスト時にランダムポートで起動可能にした
- package.jsonのtestスクリプトを `node --test test/` に更新（テストフレームワークはNode.js組み込みの `node:test` を採用）

**新たに必要になったタスク**:
- package.jsonのtestスクリプト更新（テスト実行基盤がなかったため）

### 学んだこと

**技術的な学び**:
- `geoip-lite` はrequire時にメモリ内にDBを展開するため、初回ロードに少し時間がかかる
- `node:test` はNode.js v22で安定しており、外部テストフレームワーク不要でTDDが可能
- localhostのIPv6アドレス `::1` はGeoIPで解決不能（期待通りcountry=null）

**プロセス上の改善点**:
- problems → requirements → design → tasklist の順で作成したことで、設計がブレなかった
- TDDで5テスト書いてから実装したことで、logsガードのテストも最初から含められた

### 次回への改善提案
- テスト環境のポート競合を避けるため、PORT=0パターンを最初からdesign.mdに記載する
- 手動動作確認前にサーバープロセスの有無チェック手順を含める
